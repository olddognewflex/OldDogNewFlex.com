# Pattern Index

Lookup table for all pattern files in this directory. Check here before starting any task — if a pattern exists, follow it.

| Pattern | Use when |
|---------|----------|
| [add-blog-post.md](add-blog-post.md) | Publishing a post into `learn`, `share`, or `journey` — frontmatter, slug, hero image, draft/featured |
| [add-page-route.md](add-page-route.md) | Adding a page or dynamic route under `src/pages/`, or deciding between an `.astro` component and a React island |
| [debug-build-routing.md](debug-build-routing.md) | A post 404s, a listing is missing an entry, a link points at a page the build never generated, or Zod fails the build |
| [deploy-site.md](deploy-site.md) | Publishing to production via `.deploy.sh` — read before running the irreversible `rsync --delete` |
| [giscus-comments.md](giscus-comments.md) | Touching the comment island: repo/category ids, `client:load`, or the `data-theme` bridge |
